{% extends "base.html.twig" %}

{% block title %}
    Votre fichier a été chargé !
{% endblock %}

{% block body%}
    <h1>Votre fichier a été chargé !</h1>

    <p>
    {# On accède aux propriétés / méthodes avec le point
    Twig essaie automatiquement d'appeler le getter: getNomOriginal() #}
        Le fichier <b>{{ fichier.nomOriginal}}</b> a été chargé, vous pouvez copier le lien de téléchargement ci-dessous:
    </p>

    {# On crée un lien vers la page de téléchargement ( route "download")
    L'adress nécessite un paramètre "id", on passe un tableau associatif à la fonction link
    Syntaxe des tableaux associatifs dans Twig: {cle: valeur} #}
    <input type="text" class="form-control" value="http://localhost:8000{{link('download', {id: fichier.id}) }}">
{% endblock %}